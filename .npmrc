# Make sure packages from this workspace are published with public access by default
access=public

# Use the npm registry
registry=https://registry.npmjs.org/

# Ensure dependencies are resolved from the workspace
link-workspace-packages=true

# Use hoisted node_modules
node-linker=hoisted
